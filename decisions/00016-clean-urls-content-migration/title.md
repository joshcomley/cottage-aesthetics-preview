Internal links (footer, CTAs, the header logo) migrated from `about.html`-style hrefs to
root-absolute clean URLs (`/about`) — the engine's wixy decisions/00128; legacy `.html` links
still resolve everywhere, so this is a migration, not a new validation rule.
