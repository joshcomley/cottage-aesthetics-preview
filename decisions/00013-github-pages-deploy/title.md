A new `.github/workflows/pages.yml` deploys this site to GitHub Pages on the operator's own
domain, triggered only by the `wixy-live` ref the Wixy server force-pushes on Publish/Restore
— never by an ordinary merge to `main`.
