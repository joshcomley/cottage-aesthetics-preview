`gallery.categories` becomes an admin-managed collection and `#gfilter`'s filter buttons
render from it, so Purdi can rename/add Before & After categories herself instead of a
developer editing hardcoded template markup.
