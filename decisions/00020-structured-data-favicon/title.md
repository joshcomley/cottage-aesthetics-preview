A new `_global.json.business` block feeds the wixy engine's automatic homepage JSON-LD, and
three new root files (`favicon.ico`/`.svg`, `apple-touch-icon.png`) plus per-page `<link>` tags
give the site a real favicon — closing WP0's audited `/favicon.ico` 404.
