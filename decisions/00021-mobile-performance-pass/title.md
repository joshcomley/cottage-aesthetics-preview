The `.reveal` scroll-animation mechanism is now visible-by-default with JS as a pure
progressive enhancement (4A), gallery before/after slider images are lazy-loaded alongside
the already-lazy tiles (4C), and two provably-unused font weights are trimmed (4E) — Brief
Work package 4's three site-repo pieces, measured before and after.
